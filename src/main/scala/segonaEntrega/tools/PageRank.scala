package segonaEntrega.tools

import segonaEntrega.tools.ProcessFiles.ViquipediaFile

object PageRank {
    //def calculatePageRank(viquipediaFile: ViquipediaFile, pagesThatReferenceFile: List[ViquipediaFile], totalPageNumber: Int, epsilon: Double = 1E-3, steps: Int = 5) : Double = {
    //    def tailRecursivePageRank(vf: ViquipediaFile, pages: List[ViquipediaFile], eps: Double, steps: Int, currentVal: Double): Double = {
//
    //    }
    //    // clar aquí hauríem de rebre tots els ViquipediaFile, no només el que ens ve bé.
    //    // hem de pensar bé com ho implementem això
//
    //    tailRecursivePageRank(viquipediaFile, pagesThatReferenceFile, epsilon, steps, currentVal = 1d/totalPageNumber)
    //}
}
