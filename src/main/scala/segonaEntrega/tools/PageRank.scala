package segonaEntrega.tools

import segonaEntrega.tools.ProcessFiles.ViquipediaFile

object PageRank {
    def calculatePageRank(viquipediaFile: ViquipediaFile, epsilon: Double = 1E-3, maxSteps: Int = 5) : Double = {
        // clar aquí hauríem de rebre tots els ViquipediaFile, no només el que ens ve bé.
        // hem de pensar bé com ho implementem això
    }
}
